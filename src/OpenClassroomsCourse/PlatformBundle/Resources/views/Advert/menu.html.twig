<ul class="nav nav-pills nav-stacked">
    {% for advert in adverts %}
        <li>
            <a href="{{ path('openclassroomscourse_platform_view', {'id': advert.id}) }}">
                {{ advert.title }}
            </a>
        </li>
    {% endfor %}
</ul>